[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = "-m 'not e2e'"
markers = [
    "e2e: end-to-end integration test (requires GOOGLE_API_KEY, network, ~10 min)",
]

